import * as React from 'react';
import { Wrapper } from '../../layouts/Wrapper/Wrapper';

interface UsersProps {}

export const Users: React.FunctionComponent<UsersProps> = () => {
  return <Wrapper title='Manage Users' />;
};
